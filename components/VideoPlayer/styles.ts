import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
    video:{
        alignSelf: 'center',
        width: '100%',
        aspectRatio:16/9,
    }
})
export default styles